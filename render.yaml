services:
  - type: web
    name: northcross-api
    env: python
    plan: free
    rootDir: api                # 👈 Render buscará todo dentro de /api
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn northcross_api:app --host 0.0.0.0 --port 10000
    envVars:
      - key: PORT
        value: 10000

